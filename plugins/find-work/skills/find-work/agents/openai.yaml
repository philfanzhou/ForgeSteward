interface:
  display_name: "Find Work"
  short_description: "Find actionable, independent repository work"
  default_prompt: "Use $find-work to select actionable issues and generate the execution prompt."
