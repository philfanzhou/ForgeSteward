interface:
  display_name: "Check Workflow - ForgeSteward"
  short_description: "Fill missing workflow rules and submit a PR or MR"
  default_prompt: "Use $forge-steward-check-workflow to check this repository, fill missing workflow rules and agent entry references without changing existing policies, validate, then commit, push, and open a PR or MR without merging."
