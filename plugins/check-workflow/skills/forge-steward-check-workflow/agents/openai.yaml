interface:
  display_name: "ForgeSteward Check Workflow"
  short_description: "Add missing workflow rules without replacing policy"
  default_prompt: "Use $forge-steward-check-workflow to onboard this repository by adding missing workflow constraints while preserving its existing rules."
