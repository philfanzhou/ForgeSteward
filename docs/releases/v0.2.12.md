# ForgeSteward v0.2.12

本文件是未发布目标的发布输入，不代表已经发布。相对 `v0.2.11`，本次修复 Codex 调用 prepare-work、check-workflow 和 review-and-merge 时主提示词超限的问题，并为接近上限的 execute-work、fix-feedback 留出余量；不改变业务流程、授权边界和输出契约。

## 版本对应表

| 插件 | 包版本 | 技能名 |
| --- | --- | --- |
| `check-workflow@forge-steward` | `0.2.12` | `forge-steward-check-workflow` |
| `prepare-work@forge-steward` | `0.2.12` | `forge-steward-prepare-work` |
| `execute-work@forge-steward` | `0.2.12` | `forge-steward-execute-work` |
| `review-and-merge@forge-steward` | `0.2.12` | `forge-steward-review-and-merge` |
| `fix-feedback@forge-steward` | `0.2.12` | `forge-steward-fix-feedback` |

已通过只读查询确认 `v0.2.11` Tag 与非 draft Release 均存在，因此将根目录 `VERSION`、全部插件的三份 manifest 和 Claude / ZCode 共用市场派生版本统一递增为 `0.2.12`。后续检查确认 `v0.2.12` Tag 尚不存在，继续完善同一未发布目标，不再次升版。五个插件均调整规则文件的组织和加载方式；已发布 Tag 和历史版本表保持不变。

## 原因与修复

用户报告 `Skill … exceeded the main prompt context limit and was truncated.`。与该告警匹配的 Codex 上游实现，将所选技能主提示词按 UTF-8 字节边界截到 8,000 字节后注入模型，超出时发出警告；这是单个技能主提示词的限制，不是发现列表的描述预算，也不是模型整个上下文窗口已满。核对源码基线为 `d7d9f2b9b53b53568b30c4dea7e095ec276ebe9c`：

- [render.rs 的预算与截断实现](https://github.com/openai/codex/blob/d7d9f2b9b53b53568b30c4dea7e095ec276ebe9c/codex-rs/ext/skills/src/render.rs)
- [extension.rs 的注入与告警](https://github.com/openai/codex/blob/d7d9f2b9b53b53568b30c4dea7e095ec276ebe9c/codex-rs/ext/skills/src/extension.rs)

原 prepare-work 入口为 17,800 字节，截断后无法完整注入后续 ready、交接和输出规则。原文件仍存在，模型可以通过工具补读，但不能把这当作自动保证。[官方技能文档](https://developers.openai.com/zh-Hans/docs/build-skills)介绍了通过入口、引用和资源逐步加载技能的方式。

- 入口缩为 4,120 字节，保留目标、授权、最终输出契约，新增按阶段加载要求。
- 原有详细规则移到技能内的四个文件：队列与分诊、ready 门禁、阻塞推进、交接与恢复；原有业务规则保留，跨文件引用改为相对链接。
- 路径基于实际安装的技能目录解析。工具返回截断时分段补读；文件缺失或不可读时说明影响，不凭摘要继续受影响的准备或入选判定。
- CI 对全部技能入口设 6,000 字节维护预算，为当前 8,000 字节入口上限留出余量；prepare-work 的引用文件继续保留 6,000 字节分块读取预算，不宣称是工具的通用限制。全部技能均验证引用可达性及安装后完整资源内容一致。

## 全部技能的入口检查

按完整 `SKILL.md` 的 UTF-8 字节数检查，包含 frontmatter；不是字符数或 token 数：

| 技能 | 原入口字节数 | 原状态 | 修复后字节数 |
| --- | ---: | --- | ---: |
| `check-workflow` | 8,424 | 超过 8,000 字节 | 5,291 |
| `prepare-work` | 17,800 | 超过 8,000 字节 | 4,120 |
| `execute-work` | 7,881 | 仅余 119 字节 | 5,805 |
| `review-and-merge` | 11,533 | 超过 8,000 字节 | 5,096 |
| `fix-feedback` | 7,946 | 仅余 54 字节 | 5,305 |

check-workflow 将检查、删除与提交步骤移到 `references/procedure.md`，检查前同时读取既有政策目录，并在核对其他技能时检查其引用文件。review-and-merge 将冻结与逐项审查移到 `references/review.md`，合并后操作移到 `references/post-merge.md`；审查前读取前者，执行合并前读取后者，合并门禁仍在入口。execute-work 和 fix-feedback 分别将实现/提交及逐项修复细则移到引用文件，在进入对应阶段前读取。全部入口保留授权和最终输出要求；相对路径、截断补读、文件缺失及上下文恢复规则随各技能独立分发。

引用文件通过工具读取，不会因为 `SKILL.md` 的注入上限被自动裁剪；工具本身仍可能截断输出，因此各入口要求检查完整性并补读。已有 check-workflow 政策目录为 7,716 字节，本次保持原文，不把入口预算误套到它上面。

## 迁移与使用

安装 ID、调用名称、description 和默认 prompt 不变。必须安装完整技能目录，包括 `references/`；只复制 `SKILL.md` 无法执行完整流程。发布确认后按[版本切换指南](../versioned-installation.md)升级已选插件并开始新会话；不直接改写旧版本缓存。本次源码修改不自动升级本机安装。

旧版本临时恢复时，可明确要求 Agent 从实际安装路径分段读取完整 `SKILL.md`，确认读到末尾并读取相关引用文件后再执行对应流程。该操作补充当前会话上下文，不消除旧入口在下次调用时的截断警告。

## 验证边界

执行全量单元测试、市场版本同步检查、技能校验器、相对链接检查及 `git diff --check`；实际结果记录在交付说明。五个技能的原规则迁移均做逐段完整性核对（仅跨文件引用调整），[场景矩阵](../prepare-execute-scenarios.md)补充 P29–P31。

文件预算、打包和场景条文走查不等于 Codex 模型行为端到端验证；不宣称已修改 Codex 客户端、已发布、已升级用户安装或已完成模型行为认证。发布仍须完成[发布规范](../releasing.md)中的 PR、主线 CI、Tag 和 Release 核对。
